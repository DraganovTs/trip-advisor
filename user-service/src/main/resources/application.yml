server:
  port: 8086

spring:
  application:
    name: "user-service"
  datasource:
    url: jdbc:mysql://localhost:3306/usersdb
    username: root
    password: password12345
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update